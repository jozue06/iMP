{
  "name": "imp",
  "version": "0.0.1",
  "description": "main dir",
  "author": "Jm6",
  "license": "ISC",
  "scripts": {
    "start": "npm i --prefix ./frontend && npm run build --prefix ./frontend & npm i --prefix ./backend && npm run start --prefix ./backend"
  }
}