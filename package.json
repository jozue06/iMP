{
  "name": "imp",
  "version": "0.0.12",
  "description": "main dir",
  "author": "Jm6",
  "license": "ISC",
  "scripts": {
    "start": "npm i --prefix ./backend && npm run start --prefix ./backend",
    "buildForPush": "npm run build --prefix ./frontend && npm run build --prefix ./backend"
  }
}