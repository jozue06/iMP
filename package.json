{
  "name": "imp",
  "version": "0.0.1",
  "description": "main dir",
  "author": "Jm6",
  "license": "ISC",
  "scripts": {
    "start": "npm --prefix ./front && npm i && npm startWeb && npm --prefix backend && npm i && npm start"
  }
}